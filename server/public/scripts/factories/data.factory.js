colorBlocks.factory('DataFactory', ['$http', function($http, DataFactory) {
  console.log("data factory running");
  var colors = ['red', 'blue', 'magenta', 'green', 'pink'];



  return {colors)
  };
}]);
